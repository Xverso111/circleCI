version: 2
jobs:
  install_deps:
    working_directory: ~/ec-backend
    docker:
      - image: circleci/node:erbium
    steps:
      - checkout
      - attach_workspace:
          at: ~/ec-backend
      - restore_cache:
          name: Restore Yarn Package Cache
          keys:
            - yarn-packages-{{ checksum "yarn.lock" }}
      - run:
          name: Install Dependencies
          command: yarn install --frozen-lockfile
      - save_cache:
          name: Save Yarn Package Cache
          key: yarn-packages-{{ checksum "yarn.lock" }}
          paths:
            - ~/.cache/yarn
      - persist_to_workspace:
          root: .
          paths: .
  install:
    working_directory: ~/ec-backend
    steps:
      - checkout
      - attach_workspace:
        at: ~/ec-backend
      - run:
        name: Build
        command: yarn build
workflows:
  version: 2
  build_test_deploy:
    jobs:
      - install_deps:
        context: prueba
      - install:
        context: prueba
        requires:
          - install_deps